spring:
  http:
    log-request-details: true
logging:
  level:
    reactor:
      netty:
        http:
          client:
            HttpClient: DEBUG
    org:
      springframework:
        web:
          reactive:
            function:
              client:
                ExchangeFunctions: TRACE